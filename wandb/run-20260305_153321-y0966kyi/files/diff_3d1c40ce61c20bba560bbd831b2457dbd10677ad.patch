diff --git a/oneimagetest.py b/oneimagetest.py
index f49b191..d3263d0 100644
--- a/oneimagetest.py
+++ b/oneimagetest.py
@@ -172,13 +172,6 @@ def unwrap_modelckpt(state_dict):
         new_state_dict[name] = v
     return new_state_dict
 
-def get_2d_window(size, device):
-    # Create a 1D window and expand to 2D
-    # Using a periodic cosine (Hanning-like) window
-    w1d = torch.hann_window(size, periodic=False, device=device)
-    w2d = w1d.view(-1, 1) * w1d.view(1, -1)
-    return w2d # Shape: [64, 64]
-
 def main(masked_img, masked_tensor, mask_tensor = None):
     # set random seed
     torch.manual_seed(args.seed)
@@ -218,7 +211,6 @@ def main(masked_img, masked_tensor, mask_tensor = None):
     betas = np.linspace(0.0001, 0.02, 1000, dtype=np.float64)
     betas = torch.from_numpy(betas).float().to(device)
     x_grid_mask = torch.zeros_like(x_rand, device=device)
-    # window = get_2d_window(p_size, device)
     for (hi, wi) in corners:
         x_grid_mask[:, :, hi:hi + p_size, wi:wi + p_size] += 1
     xs, gif_frames = generalized_steps_overlapping(x_rand, masked_img, masked_tensor, mask_tensor, seq, model, betas, x_grid_mask, eta=0.,
diff --git a/repaint_implementation.py b/repaint_implementation.py
index 2e43335..e4d96d4 100644
--- a/repaint_implementation.py
+++ b/repaint_implementation.py
@@ -99,7 +99,7 @@ class GaussianDiffusionRepaintWD_modded:
                 +
                 (1 - gt_keep_mask) * (x)
             )
-            to_pil_image(x.squeeze().cpu()).save("outputs/WeatherDiff64/stitched_image_t"+str(t.item())+".png")
+            # to_pil_image(x.squeeze().cpu()).save("outputs/WeatherDiff64/stitched_image_t"+str(t.item())+".png")
 
         out = self.p_mean_variance(
             model,
@@ -116,7 +116,7 @@ class GaussianDiffusionRepaintWD_modded:
         return result
 
     def get_wd_processed_output(self, model, x, t, gt):
-        manual_batching_size = 64
+        manual_batching_size = 1024#len(self.corners)
         patch_pbar = tqdm(range(0, len(self.corners), manual_batching_size), 
                         leave=False, 
                         desc=f"Processing Patches (t={t.item()})")
@@ -135,6 +135,18 @@ class GaussianDiffusionRepaintWD_modded:
         et = th.div(et_output, self.x_grid_mask)
         return et
     
+    def dynamic_thresholding(self,pred_xstart, p=0.995, s=1.0):
+        """
+        Clamps the signal but scales it back if values exceed 's'.
+        """
+        # Calculate the 'p'-th percentile absolute value
+        s_thresh = th.quantile(th.abs(pred_xstart), p)
+        s_thresh = th.maximum(s_thresh, th.tensor(s).to(s_thresh.device))
+        
+        # Clamp to [-s_thresh, s_thresh] and then normalize back to [-s, s]
+        pred_xstart = th.clamp(pred_xstart, -s_thresh, s_thresh) / (s_thresh / s)
+        return pred_xstart
+
     def p_mean_variance(
             self,
             model,
@@ -170,6 +182,7 @@ class GaussianDiffusionRepaintWD_modded:
 
             #x0_t
             pred_xstart = (x - model_output * (1 - self.alpha_cumprod).sqrt()) / self.alpha_cumprod.sqrt()
+            pred_xstart = self.dynamic_thresholding(pred_xstart)#.clamp(-1, 1)#explodes for jumpbacks>=5
             wandb.log({
                 "timestep": t.item(),
                 "range/pred_xstart_min": pred_xstart.min().item(),
@@ -220,7 +233,7 @@ class GaussianDiffusionRepaintWD_modded:
             sample_idxs = defaultdict(lambda: 0)
             self.alpha_cumprod = self.compute_alpha(t_last.long())#to predict clean image
             self.alpha_cumprod_prev = self.compute_alpha(t_cur.long())#to determine how much clean signal to put back in, get the amount of noise to add in for the next step
-            for _ in range(jump_n_samples):
+            for jumps in range(jump_n_samples):
                 # if t_cur < t_last:  # reverse
                 #DENOISE step
                 # print('Denoising step')
@@ -260,7 +273,7 @@ class GaussianDiffusionRepaintWD_modded:
                     "range/xt_jumpback_mean": image_after_step.mean().item(),
                     "range/xt_jumpback_std": image_after_step.std().item(),
                 })
-                
+            print("Repainting jumps done", jumps)  
             return xt_next
 
 
diff --git a/reverse_diffusion.gif b/reverse_diffusion.gif
index 1104e99..6bd356d 100644
Binary files a/reverse_diffusion.gif and b/reverse_diffusion.gif differ
diff --git a/utils/__init__.py b/utils/__init__.py
index e1addac..9ba8240 100644
--- a/utils/__init__.py
+++ b/utils/__init__.py
@@ -1,3 +1,3 @@
-from utils.logging import *
+from utils.logging_wd import *
 from utils.sampling import *
 from utils.optimize import *
diff --git a/utils/metrics.py b/utils/metrics.py
index 258a594..585e817 100644
--- a/utils/metrics.py
+++ b/utils/metrics.py
@@ -165,8 +165,8 @@ def _convert_output_type_range(img, dst_type):
 import cv2
 import torch
 if __name__ == '__main__':
-    img1 = cv2.imread('../../0_clean.png')
-    img2 = cv2.imread('../misc/0_clean_j5_s50.png')
+    img1 = cv2.imread('../qual_tests/0_clean.png')
+    img2 = cv2.imread('../outputs/WeatherDiff64/WeatherDiff64/1jr8_0_clean.png')
     psnr = calculate_psnr(img1, img2)
     ssim = calculate_ssim(img1, img2)
     print(f'PSNR: {psnr:.4f} dB')
diff --git a/wandb/debug-internal.log b/wandb/debug-internal.log
index 409f930..1e64abe 120000
--- a/wandb/debug-internal.log
+++ b/wandb/debug-internal.log
@@ -1 +1 @@
-run-20260304_164742-siyvzxic/logs/debug-internal.log
\ No newline at end of file
+run-20260305_153321-y0966kyi/logs/debug-internal.log
\ No newline at end of file
diff --git a/wandb/debug.log b/wandb/debug.log
index 3b1b9bf..4800f88 120000
--- a/wandb/debug.log
+++ b/wandb/debug.log
@@ -1 +1 @@
-run-20260304_164742-siyvzxic/logs/debug.log
\ No newline at end of file
+run-20260305_153321-y0966kyi/logs/debug.log
\ No newline at end of file
diff --git a/wandb/latest-run b/wandb/latest-run
index 7b78b79..49dd2f5 120000
--- a/wandb/latest-run
+++ b/wandb/latest-run
@@ -1 +1 @@
-run-20260304_164742-siyvzxic
\ No newline at end of file
+run-20260305_153321-y0966kyi
\ No newline at end of file
diff --git a/wandb/run-20260304_164742-siyvzxic/files/output.log b/wandb/run-20260304_164742-siyvzxic/files/output.log
index b5a3ba3..2c75d3b 100644
--- a/wandb/run-20260304_164742-siyvzxic/files/output.log
+++ b/wandb/run-20260304_164742-siyvzxic/files/output.log
@@ -45,3 +45,15 @@ Repainting step, t_cur: 260.0 t_last: 280.0
 Repainting step, t_cur: 240.0 t_last: 260.0
 Repainting step, t_cur: 220.0 t_last: 240.0
 Repainting step, t_cur: 200.0 t_last: 220.0
+Repainting step, t_cur: 180.0 t_last: 200.0
+Repainting step, t_cur: 160.0 t_last: 180.0
+Repainting step, t_cur: 140.0 t_last: 160.0
+Repainting step, t_cur: 120.0 t_last: 140.0
+Repainting step, t_cur: 100.0 t_last: 120.0
+Repainting step, t_cur: 80.0 t_last: 100.0
+Repainting step, t_cur: 60.0 t_last: 80.0
+Repainting step, t_cur: 40.0 t_last: 60.0
+Repainting step, t_cur: 20.0 t_last: 40.0
+Repainting step, t_cur: 0.0 t_last: 20.0
+Repainting step, t_cur: -1.0 t_last: 0.0
+Xs range tensor(-1.0530, device='cuda:0') tensor(1.0819, device='cuda:0')
diff --git a/wandb/run-20260304_164742-siyvzxic/run-siyvzxic.wandb b/wandb/run-20260304_164742-siyvzxic/run-siyvzxic.wandb
index f12b253..9a09462 100644
Binary files a/wandb/run-20260304_164742-siyvzxic/run-siyvzxic.wandb and b/wandb/run-20260304_164742-siyvzxic/run-siyvzxic.wandb differ
